# Render Blueprint — backend only. Set SARVAM_API_KEY (and optionally SUPABASE_*) in Dashboard.
services:
  - type: web
    name: patchup-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
